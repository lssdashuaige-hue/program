# PAS-027 Gate C2 hosted read-only attempt — 20260821-01

## Decision

**GATE_C2_FAIL_CLOSED / NOT_PASSED / ONLINE_ORDINAL_CONSUMED**

The fail-closed control worked, but Gate C2 did not pass. The attempt stopped
at the first hosted read-only fingerprint before either Supabase migration CLI
business command ran. This result is not migration-apply authorization and is
not evidence that the hosted dry-run proposes only 0006 and 0007.

## Frozen execution identity

- Repository HEAD at execution:
  `194f9da4f40a48cf3fd22ebcae0ffeb3d2936b41`
- Repository clean at execution: `true`
- Online ordinal: `20260821-01`
- Run ID: `20260821T171142093Z-dee063b8`
- Started: `2026-08-21T17:11:42.1994147Z`
- Finished: `2026-08-21T23:17:50.6089366Z`
- Runner SHA-256:
  `19965f4b59b1ec2ecc5105a7fa27c29d7aaf6391fd1caf33432564b82531f0d6`
- Execution-contract SHA-256:
  `9a2f6bad1be3efcf0384d3407b9dcd2befbc88d2d8ca1bf3ec1a99c5364683a8`
- Atomic result SHA-256:
  `3957653fdb11a7aa1f93d7a3773b0e199fd3da4cf0ec540f88f64548875be2d5`
- Online-freeze SHA-256:
  `df5d7be80405ef9f437d10771a263ffad4ee5918f394c5808122f042f3e3e406`

The protected local evidence is stored outside Git under
`pas-gate-c2-work/evidence-20260821T171142093Z-dee063b8-online/`. The atomic
result and freeze marker have protected ACLs limited to the local user and
SYSTEM.

## Observed outcome

- Status: `FAIL_CLOSED`
- Conclusion: `GATE_C2_INDETERMINATE_OR_BLOCKED_NO_RETRY`
- Failure stage: `fingerprint_before`
- Sanitized error: `Hosted fingerprint-before fingerprint failed.`
- Hosted contact attempted: `true`
- Successful before snapshot: `false`
- Gate B hosted-baseline comparison reached: `false`
- `migration list` attempts: `0`
- `db push --dry-run` attempts: `0`
- Failure-after audit attempts: `0`
- Migration apply attempts: `0`
- Migration repair attempts: `0`
- Deployment attempts: `0`
- DeepSeek attempts: `0`
- `before_after_identical`: `null`
- Proposed migrations: not observed
- Password persisted: `false`
- Raw CLI output persisted: `false`

Migration 0006 and 0007 were not applied. The frozen runner issued no hosted
write-capable or migration business command. Its only hosted attempt was the
fingerprint path with a forced read-only PostgreSQL session, so the runner's
narrow `hosted_state_modified=false` statement is supported for actions taken
by this execution. It must not be restated as a successful before/after proof
that the hosted database was globally unchanged: no successful before snapshot
exists and `before_after_identical` is `null`.

The local negative control did intentionally try a non-dry-run migration only
against a disposable local PostgreSQL 17 database. Read-only mode rejected it
with SQLSTATE `25006`, and its local before/after fingerprint was identical.
That control never contacted the hosted project.

## Diagnostic limit

The online freeze was recorded at `2026-08-21T23:17:44.8316496Z` and the final
FAIL_CLOSED result at `2026-08-21T23:17:50.6089366Z`, a 5.777-second
post-freeze interval. The sanitized error proves that the Docker child exited
non-zero, but no child-completion timestamp or numeric exit code was preserved.
The frozen runner scanned the child output for the password but did not preserve
a safe phase marker, SQLSTATE, or allowlisted failure category before discarding
raw output. The evidence therefore cannot distinguish authentication, TLS,
DNS/refused, pooler startup options, bootstrap, or hosted fingerprint SQL/guard
failures. Assigning a unique cause would be speculation.

## Required next gate

Ordinal `20260821-01` is terminal and must not be retried, repaired, deleted, or
rewritten. Before any new hosted attempt:

1. add offline-only, secret-safe phase markers, exit code, SQLSTATE, hashes and
   allowlisted failure classification to a new runner;
2. prove with local fixtures that raw, escaped, encoded, and normalized password
   variants cannot reach diagnostic evidence;
3. pass a hosted-free LocalPreflight and independently review the new runner,
   contract, ACLs and hashes;
4. freeze a new repository HEAD, runner, execution contract and online ordinal;
5. obtain new explicit authorization for one diagnostic-only hosted read-only
   fingerprint attempt, with no migration list, dry-run, apply, repair or
   deployment command.

Only after that diagnostic succeeds and its cause is resolved may a separately
authorized new complete Gate C2 attempt be considered. Migration and deployment
remain blocked.
